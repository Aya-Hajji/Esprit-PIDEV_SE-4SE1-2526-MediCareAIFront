const API_PORT = 8090;

export const environment = {
  production: true,
  apiPort: API_PORT,
  apiUrl: `http://localhost:${API_PORT}/MediCareAI`,
  devAuthToken: '',
  appUrl: '',
  openaiApiKey: ''
};
